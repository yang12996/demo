一、类\用例前置（夹具）:
       1、方法准备
               类前置：
     	   添加类装饰器：@classmethod
        	   定义的方法为： setUpClass
                用例前置：
                    定义的方法为： setUp

        1、准备登录数据
                url
                请求参数
                请求头

        2、实现请求接口
                 使用requests

        3、取关联接口的值
                   采用正则取值("$":取根节点，"..":就是不管位置，选择所有符合条件的条件,"[0]":取值)
                    把值设为类属性

编写方法：
      1、准备数据
               接口地址、接口请求参数、预期结果、请求头（转成小写）

       2、请求接口，返回实际结果
                使用：requests.request
                返回的实际结果用于断言

       3、数据库校验查询,例如充值接口，利用充值后-充值前的值，需要断言变化为0

       4、断言
              使用unittest继承的断言assertEqual
